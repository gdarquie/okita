{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {% if character.gender == 'F'  %}
            {% set pronom = 'elle' %}
            {% set defini = 'la' %}
            {% set indefini = 'une' %}
        {% else %}
            {% set pronom = 'il' %}
            {% set defini = 'le' %}
            {% set indefini = 'un' %}
        {% endif %}

        <h1>Liste des actions de {{ character.name }}</h1>
        <p><a href='{{ path('get_character', {'id': character.id}) }}'>Voir le personnage</a></p>

        {% for action in actions %}
            <article>
                <p>{{ action.title |capitalize }} entre {{ action.startAt }} et {{ action.endAt }}.</p>
            </article>
        {% endfor %}
    </div>
{% endblock %}